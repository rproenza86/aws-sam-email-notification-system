{
    "name": "sendemails",
    "version": "1.0.0",
    "scripts": {
        "build": "tsc",
        "invoke": "npm run build && npm run invoke:only",
        "invoke:only": "stackery local invoke -e test --aws-profile default"
    },
    "devDependencies": {
        "aws-sdk": "~2",
        "@types/aws-sdk": "^2.7.0",
        "@types/node": "^12.6.8",
        "typescript": "^3.5.3"
    }
}
